change_id: issue-485
generated_at: "2026-04-11T13:19:44Z"
test_framework: pytest
scenarios:
  # -------------------------------------------------------------------------
  # Requirement: auto-init 抑制ガード（Phase 1）
  # -------------------------------------------------------------------------
  - id: "nested-root-suppresses-auto-init"
    name: "nested root 存在時に auto-init を発動しない"
    spec_file: "specs/auto-init-suppression/spec.md"
    spec_line: 6
    requirement: "auto-init 抑制ガード（Phase 1）"
    test_file: "cli/twl/tests/scenarios/test_issue_485_auto_init_suppression.py"
    test_name: "TestAutoInitSuppressedWhenNestedRootExists"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: >
      cmd_new は DeltaspecNotFound 後に git ls-tree origin/main を呼び出す実装が
      まだないため、現在は auto-init が実行されてしまう。
      実装完了後に passing に更新すること。

  - id: "allow-auto-init-with-env-var"
    name: "TWL_SPEC_ALLOW_AUTO_INIT=1 で従来動作を維持"
    spec_file: "specs/auto-init-suppression/spec.md"
    spec_line: 10
    requirement: "auto-init 抑制ガード（Phase 1）"
    test_file: "cli/twl/tests/scenarios/test_issue_485_auto_init_suppression.py"
    test_name: "TestAutoInitAllowedWithEnvVar"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: >
      env var チェック自体は実装完了後にはじめて意味を持つ。
      現在はガードがないため、env var なしでも auto-init が走る（テストは偽陽性 pass）。

  # -------------------------------------------------------------------------
  # Requirement: origin/main アクセス失敗時のフォールバック
  # -------------------------------------------------------------------------
  - id: "offline-fallback"
    name: "offline 環境でのフォールバック"
    spec_file: "specs/auto-init-suppression/spec.md"
    spec_line: 17
    requirement: "origin/main アクセス失敗時のフォールバック"
    test_file: "cli/twl/tests/scenarios/test_issue_485_auto_init_suppression.py"
    test_name: "TestOfflineFallback"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: >
      WARN メッセージ出力テストは失敗する（実装前）。
      auto-init 継続自体は現在もデフォルト動作として pass。

  # -------------------------------------------------------------------------
  # Requirement: find_deltaspec_root エラーメッセージ強化
  # -------------------------------------------------------------------------
  - id: "detailed-error-walk-up-walk-down-fail"
    name: "walk-up/walk-down 両方失敗時の詳細エラー"
    spec_file: "specs/auto-init-suppression/spec.md"
    spec_line: 26
    requirement: "find_deltaspec_root エラーメッセージ強化"
    test_file: "cli/twl/tests/scenarios/test_issue_485_paths_error_message.py"
    test_name: "TestFindDeltaspecRootDetailedError"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: >
      paths.py の DeltaspecNotFound メッセージが現時点では簡素。
      Walked up from / Searched git root / Hint: の3要素を含む実装が必要。

  # -------------------------------------------------------------------------
  # Requirement: chain-runner step_init rebase ガード
  # -------------------------------------------------------------------------
  - id: "chain-runner-warn-when-nested-config-missing"
    name: "nested config.yaml 欠落時に WARN を出力"
    spec_file: "specs/auto-init-suppression/spec.md"
    spec_line: 33
    requirement: "chain-runner step_init rebase ガード"
    test_file: "cli/twl/tests/scenarios/test_issue_485_chain_runner_step_init.py"
    test_name: "TestStepInitNestedConfigWarnWhenMissing"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: >
      guard ロジックをラッパースクリプト経由でテスト。
      chain-runner.sh の step_init 本体への統合実装後に passing に更新すること。

  - id: "chain-runner-no-warn-when-both-configs-exist"
    name: "両 config.yaml 存在時は WARN なし"
    spec_file: "specs/auto-init-suppression/spec.md"
    spec_line: 37
    requirement: "chain-runner step_init rebase ガード"
    test_file: "cli/twl/tests/scenarios/test_issue_485_chain_runner_step_init.py"
    test_name: "TestStepInitNoWarnWhenBothConfigsExist"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: null

  # -------------------------------------------------------------------------
  # Requirement: unit test — nested root 存在時の auto-init 抑制（spec 自体の要件）
  # -------------------------------------------------------------------------
  - id: "test-new-auto-init-suppressed-when-nested-root-exists"
    name: "test_new_auto_init_suppressed_when_nested_root_exists"
    spec_file: "specs/auto-init-suppression/spec.md"
    spec_line: 46
    requirement: "unit test — nested root 存在時の auto-init 抑制"
    test_file: "cli/twl/tests/scenarios/test_issue_485_auto_init_suppression.py"
    test_name: "TestAutoInitSuppressedWhenNestedRootExists::test_spec_new_auto_init_suppressed_when_nested_root_exists"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: >
      new.py に git ls-tree チェックと抑制ロジックが未実装のため失敗する。

  - id: "test-new-auto-init-allowed-with-env-var"
    name: "test_new_auto_init_allowed_with_env_var"
    spec_file: "specs/auto-init-suppression/spec.md"
    spec_line: 50
    requirement: "unit test — nested root 存在時の auto-init 抑制"
    test_file: "cli/twl/tests/scenarios/test_issue_485_auto_init_suppression.py"
    test_name: "TestAutoInitAllowedWithEnvVar::test_spec_new_auto_init_allowed_with_env_var"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: null
