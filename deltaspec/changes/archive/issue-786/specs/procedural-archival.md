# Procedural Change: DeltaSpec Archival

This change introduces no new spec requirements.
The work consists of completing issue-729 artifacts and archiving 4 pending DeltaSpec changes.
Archive this change with `--skip-specs` flag.
