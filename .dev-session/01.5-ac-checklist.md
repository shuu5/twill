## 受け入れ基準（Issue #170）

1. AC1: `generate_window_name` が bats テスト全ケースで期待値を返す
2. AC2: `cld-spawn` / `cld-fork` が新命名（`wt-` / `fk-` prefix）を生成する
3. AC3: co-autopilot orchestrator が同じ命名規則を使う（`ap-` prefix）
4. AC4: 同一 canonical_context の 2 回目 spawn で既存 window を select-window で再利用する
5. AC5: 並列 2 spawn の TOCTOU テストが 100 回実行して race を起こさない
6. AC6: bare+worktree 構成で repo 名が branch と混同されない
7. AC7: detached HEAD / 長大 branch / 日本語 branch / 非 git dir のすべてで安全に動作
8. AC8: `plugins/session/CLAUDE.md` に命名規則と slug 仕様が追記される
9. AC9: `session-state.sh wait` / `session-comm.sh inject-file` の window_name 指定が引き続き動作する
