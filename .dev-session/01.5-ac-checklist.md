## 受け入れ基準（Issue #6）

1. AC1: `twl check`, `twl validate` 等の全サブコマンドが動作する
2. AC2: `twl chain generate <name>` のネストされたサブコマンドが動作する
3. AC3: `twl --help` でサブコマンド一覧が表示される
4. AC4: twl-engine.py がリポジトリから削除されている
5. AC5: bash wrapper が3行以内である
6. AC6: `pytest tests/` で全テストが通過する
7. AC7: `twl update-svgs` が動作する
