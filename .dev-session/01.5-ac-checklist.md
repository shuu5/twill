## 受け入れ基準（Issue #789）

1. `ref-invariants.md` を parse して bats テストを生成するスクリプトを作成（`scripts/generate-invariants-bats.sh`）
   - 入力: `ref-invariants.md` の不変条件定義 + 検証方法（bats テスト名リンク）
   - 出力: 網羅性チェック（--check）またはスタブ追記（--generate）
2. A-M **全 13 個の不変条件に対応する bats テスト**が存在
3. 動的生成を CI で自動化（`.github/workflows/invariants-coverage.yml` — `ref-invariants.md` 変更時に確認）
4. 既存の A-C テストの振る舞いは保持（regression なし）
5. `bats tests/bats/invariants/` 全 PASS（既存の F/H/K 失敗は pre-existing）
6. `twl validate` PASS（`twl --check` で全ファイル存在確認）
