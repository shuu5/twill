# ac-test-mapping.yaml - Issue #960 (retained) + Issue #999
# AC -> test mapping (TDD RED phase)
# Generated by ac-scaffold-tests agent
issue: 960
framework: bats
mappings:
  - ac_index: 1
    ac_text: "L235/L250 の if gh issue view ... | grep -Fxq パターンを capture 形式に書き換える（SIGPIPE false-negative 修正）"
    test_file: "plugins/twl/tests/bats/autopilot-launch-status-gate.bats"
    test_name: "ac1_s2: 実装が capture 形式になっている（pipe 形式が除去済み）"
    status: RED
    red_mechanism: "現行実装 L235/L250 に gh...| grep パターンが存在するため grep がマッチし行数>0 → FAIL"

  - ac_index: 2
    ac_text: "Board取得失敗path で gh が exit 141(SIGPIPE) かつ refined あり → ALLOW_LABEL_FALLBACK で exit 0"
    test_file: "plugins/twl/tests/bats/autopilot-launch-status-gate.bats"
    test_name: "ac2_s2: Board取得失敗path — gh が exit 141(SIGPIPE) かつ refined あり → ALLOW_LABEL_FALLBACK で exit 0"
    status: RED
    red_mechanism: "pipefail 下で pipeline が exit 141 → if condition false → has_label=0 → DENY (exit 1) になるが test は assert_success を期待"

  - ac_index: 2
    ac_text: "Board取得失敗path で gh が exit 141 かつ refined あり → gate.log に ALLOW_LABEL_FALLBACK が記録される"
    test_file: "plugins/twl/tests/bats/autopilot-launch-status-gate.bats"
    test_name: "ac2_s2: Board取得失敗path — gh が exit 141(SIGPIPE) かつ refined あり → gate.log に ALLOW_LABEL_FALLBACK が記録される"
    status: RED
    red_mechanism: "false-negative で ALLOW_LABEL_FALLBACK に到達しないため gate.log に記録されない"

  - ac_index: 2
    ac_text: "Board未登録path で gh が exit 141(SIGPIPE) かつ refined あり → ALLOW_LABEL_FALLBACK で exit 0"
    test_file: "plugins/twl/tests/bats/autopilot-launch-status-gate.bats"
    test_name: "ac2_s2: Board未登録path — gh が exit 141(SIGPIPE) かつ refined あり → ALLOW_LABEL_FALLBACK で exit 0"
    status: RED
    red_mechanism: "L250 でも同様の false-negative が発生"

  - ac_index: 2
    ac_text: "Board未登録path で gh が exit 141 かつ refined あり → gate.log に ALLOW_LABEL_FALLBACK が記録される"
    test_file: "plugins/twl/tests/bats/autopilot-launch-status-gate.bats"
    test_name: "ac2_s2: Board未登録path — gh が exit 141(SIGPIPE) かつ refined あり → gate.log に ALLOW_LABEL_FALLBACK が記録される"
    status: RED
    red_mechanism: "false-negative で ALLOW_LABEL_FALLBACK に到達しないため gate.log に記録されない"

  - ac_index: 6
    ac_text: "bash -n および shellcheck が新規 warning を出さない"
    test_file: "plugins/twl/tests/bats/autopilot-launch-status-gate.bats"
    test_name: "ac6_s2: shellcheck が新規 warning を出さない"
    status: GREEN
    note: "実装後の回帰防止テスト。shellcheck 未インストール時は skip"

# --- Issue #999 ---
issue_999:
  issue: 999
  framework: bats
  mappings:
    - ac_index: 1
      ac_text: "autopilot-cleanup.sh 実行後 .autopilot/session.json が存在しない — is_session_completed=true の場合"
      test_file: "plugins/twl/tests/unit/cleanup-session-json-archive/cleanup-session-json-archive.bats"
      test_name: "ac1: all issues done → session.json が archive へ移動され存在しない"
      status: GREEN
    - ac_index: 2
      ac_text: "新規 Pilot 起動時に「既存セッション競合」エラーが出ない (Wave N → Wave N+1 遷移)"
      test_file: "plugins/twl/tests/unit/cleanup-session-json-archive/cleanup-session-json-archive.bats"
      test_name: "ac6-positive: Wave 完了後 cleanup → session.json が archive される"
      status: GREEN
    - ac_index: 3
      ac_text: "archive ディレクトリに session.json が保存される (audit trail 保持)"
      test_file: "plugins/twl/tests/unit/cleanup-session-json-archive/cleanup-session-json-archive.bats"
      test_name: "ac3: all issues done → archive ディレクトリに session.json が保存される"
      status: GREEN
    - ac_index: 4
      ac_text: "is_session_completed=false の場合は session.json を archive せず警告"
      test_file: "plugins/twl/tests/unit/cleanup-session-json-archive/cleanup-session-json-archive.bats"
      test_name: "ac4: running issue あり → session.json は archive せず保持、警告を出す"
      status: GREEN
    - ac_index: 5
      ac_text: "既存の issue state file archive 動作は影響なし (regression なし)"
      test_file: "plugins/twl/tests/unit/cleanup-session-json-archive/cleanup-session-json-archive.bats"
      test_name: "ac5-regression: done issue は引き続き archive される"
      status: GREEN
    - ac_index: 6
      ac_text: "bats test: Wave 完了 → session.json archive (positive)"
      test_file: "plugins/twl/tests/unit/cleanup-session-json-archive/cleanup-session-json-archive.bats"
      test_name: "ac6-positive: Wave 完了後 cleanup → session.json が archive される"
      status: GREEN
    - ac_index: 7
      ac_text: "bats test: in-progress session → archive されない (negative)"
      test_file: "plugins/twl/tests/unit/cleanup-session-json-archive/cleanup-session-json-archive.bats"
      test_name: "ac6-negative: in-progress Wave の cleanup → session.json は archive されない"
      status: GREEN
