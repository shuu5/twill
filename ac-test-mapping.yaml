# ac-test-mapping.yaml - Issue #973
# AC -> test mapping (TDD RED phase)
# Generated by ac-scaffold-tests agent
issue: 973
framework: bats+pytest
mappings:
  - ac_index: 1
    ac_text: "cld-observe-any の emit_event を拡張し prompt_context / options フィールドを追加する"
    test_file: "plugins/twl/tests/bats/refs/permission-ui-docs.bats"
    test_name: "AC1: emit_event に prompt_context フィールドが追加されている (RED: 未実装)"
    status: RED
    red_mechanism: "emit_event に prompt_context フィールドが存在しないため grep が失敗し fail する"

  - ac_index: 2
    ac_text: "intervene-auto.md に --pattern permission-ui-response を追加し soft_deny_match.py で分岐する"
    test_file: "cli/twl/tests/test_soft_deny_match.py"
    test_name: "TestNoMatch::test_ac2_read_file_no_match"
    status: RED
    red_mechanism: "twl.intervention.soft_deny_match モジュールが存在しないため ImportError で fail"

  - ac_index: 3
    ac_text: "soft-deny-rules.md を新設し schema_version: 1 と 5 ルール (code-from-external / irreversible-local-destruction / memory-poisoning / secret-exfiltration / privilege-escalation) を定義する"
    test_file: "plugins/twl/tests/bats/refs/permission-ui-docs.bats"
    test_name: "AC3: soft-deny-rules.md が存在する (RED: 未作成)"
    status: RED
    red_mechanism: "soft-deny-rules.md が存在しないため [ -f ] が失敗し fail する"

  - ac_index: 4
    ac_text: "bats integration test — safe (Read file) → inject '1' + InterventionRecord"
    test_file: "plugins/twl/tests/bats/commands/permission-ui-response-safe.bats"
    test_name: "AC4-safe: safe prompt → soft_deny_match が no-match を返す (RED: 未実装)"
    status: RED
    red_mechanism: "soft_deny_match モジュール未実装のため python3 -m twl.intervention.soft_deny_match が失敗"

  - ac_index: 4
    ac_text: "bats integration test — deny-confirm (curl|bash) → STOP + Layer 1 Confirm"
    test_file: "plugins/twl/tests/bats/commands/permission-ui-response-deny-confirm.bats"
    test_name: "AC4-deny-confirm: curl|bash → soft_deny_match が match-confirm を返す (RED: 未実装)"
    status: RED
    red_mechanism: "soft_deny_match モジュール未実装のため python3 -m twl.intervention.soft_deny_match が失敗"

  - ac_index: 4
    ac_text: "bats integration test — deny-escalate (sudo) → STOP + Layer 2 Escalate"
    test_file: "plugins/twl/tests/bats/commands/permission-ui-response-deny-escalate.bats"
    test_name: "AC4-deny-escalate: sudo prompt → soft_deny_match が match-escalate を返す (RED: 未実装)"
    status: RED
    red_mechanism: "soft_deny_match モジュール未実装のため python3 -m twl.intervention.soft_deny_match が失敗"

  - ac_index: 5
    ac_text: "ドキュメント整合性 — pitfalls-catalog / monitor-channel-catalog / intervention-catalog パターン14 / observation-pattern-catalog 更新"
    test_file: "plugins/twl/tests/bats/refs/permission-ui-docs.bats"
    test_name: "AC5: intervention-catalog にパターン 14 が追加されている (RED: 未追加)"
    status: RED
    red_mechanism: "intervention-catalog.md にパターン 14 が存在しないため grep が失敗し fail する"

  - ac_index: 6
    ac_text: "regex 正確性 — 既知 4 prompt 一致 / 3 false positive 非一致 / ANSI 残留 non-match / state=unknown --force"
    test_file: "cli/twl/tests/test_soft_deny_match.py"
    test_name: "TestAC6RegexAccuracy::test_ac6_match_yes_proceed"
    status: RED
    red_mechanism: "twl.intervention.soft_deny_match モジュールが存在しないため ImportError で fail"

  - ac_index: 7
    ac_text: "ADR-014 3 層 Auto/Confirm/Escalate 分類一貫性 verify"
    test_file: "cli/twl/tests/test_soft_deny_match.py"
    test_name: "TestMatchEscalate::test_ac7_match_escalate_is_layer2"
    status: RED
    red_mechanism: "twl.intervention.soft_deny_match モジュールが存在しないため ImportError で fail"
