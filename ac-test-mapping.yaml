# ac-test-mapping.yaml - Issue #978
# AC -> test mapping (TDD RED phase)
# Generated by ac-scaffold-tests agent
issue: 978
framework: bats
mappings:
  - ac_index: 1
    ac_text: "is_session_completed() が .autopilot/issues/issue-*.json の status を集約判定する（ABI: session_file → autopilot_dir）"
    test_file: "plugins/twl/tests/bats/scripts/is-session-completed.bats"
    test_name: "ac1: is_session_completed の第1引数は autopilot_dir である（ABI 変更後）"
    status: RED
    red_mechanism: "現行実装は 'local session_file=$1' であるため [[ $sig == *autopilot_dir* ]] が失敗"

  - ac_index: 1
    ac_text: "呼出元 autopilot-init.sh:80 が autopilot_dir を渡す形式になっている"
    test_file: "plugins/twl/tests/bats/scripts/is-session-completed.bats"
    test_name: "ac1: 呼出元 L80 付近が autopilot_dir を渡す形式になっている"
    status: RED
    red_mechanism: "現行実装は is_session_completed \"$SESSION_FILE\" のため callsite に AUTOPILOT_DIR が含まれない"

  - ac_index: 2
    ac_text: "bats test: case3 — issues/ dir + 全 issue-*.json が status=done → true（自動削除発火）"
    test_file: "plugins/twl/tests/bats/scripts/is-session-completed.bats"
    test_name: "ac2-case3: 全 issue-*.json が status=done → is_session_completed が true（自動削除が発火）"
    status: RED
    red_mechanism: "現行実装は session.json.issues[] を参照するため per-issue file のみ存在する fixture では false を返す"

  - ac_index: 2
    ac_text: "bats test: case3 補足 — 全 done → 自動削除後に issue-*.json も削除される"
    test_file: "plugins/twl/tests/bats/scripts/is-session-completed.bats"
    test_name: "ac2-case3: 全 done → 自動削除後に issue-*.json も削除される"
    status: RED
    red_mechanism: "現行実装は per-issue file を見ないため is_session_completed が false → 自動削除しない"

  - ac_index: 3
    ac_text: "既存テスト更新: autopilot-init.bats:221 を新仕様に書き換え（#732 comment 継承）"
    test_file: "plugins/twl/tests/bats/scripts/autopilot-init.bats"
    test_name: "issues/ dir absent + --force: blocks with exit 1 (fail-closed, not treated as done)"
    status: GREEN
    note: "新旧実装どちらも同じ結果のためリグレッション確認テスト。実際の RED/GREEN は AC1/2 で担保"

  - ac_index: 3
    ac_text: "既存テスト更新: session-auto-cleanup.bats:188-241 の 3 件を書き換え（#732 comment 継承）"
    test_file: "plugins/twl/tests/unit/session-auto-cleanup/session-auto-cleanup.bats"
    test_name: "session-auto-cleanup[ac1b]: issues/ dir 存在 + ファイル 0 件 → 完了済みとみなさず exit 1 で停止"
    status: GREEN
    note: "新旧実装どちらも同じ結果のためリグレッション確認テスト"

  - ac_index: 4
    ac_text: "integration test: 全 issue done → session.json が自動削除される"
    test_file: "plugins/twl/tests/integration/autopilot-init-cleanup.bats"
    test_name: "integration[ac4]: 全 issue done → session.json が自動削除される"
    status: RED
    red_mechanism: "現行実装は session.json.issues[] を参照するため per-issue file のみの fixture で is_session_completed が false"

  - ac_index: 4
    ac_text: "integration test: 全 issue done → issues/issue-*.json が全て削除される"
    test_file: "plugins/twl/tests/integration/autopilot-init-cleanup.bats"
    test_name: "integration[ac4]: 全 issue done → issues/issue-*.json が全て削除される"
    status: RED
    red_mechanism: "同上"

  - ac_index: 4
    ac_text: "integration test: wave-b-final 相当の残留セッション → 全 done で自動削除"
    test_file: "plugins/twl/tests/integration/autopilot-init-cleanup.bats"
    test_name: "integration[ac4]: wave-b-final 相当の残留セッション → 全 done で自動削除"
    status: RED
    red_mechanism: "同上"
