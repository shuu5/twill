change_id: orchestrator-nudge-command
generated_at: "2026-04-03"

unit:
  - file: tests/bats/scripts/orchestrator-nudge.bats
    covers:
      - "nudge: 'setup chain 完了' → /twl:workflow-test-ready #N"
      - "nudge: '>>> 提案完了' → 空（chain 内遷移）"
      - "nudge: 'テスト準備.*完了' → /twl:workflow-pr-cycle #N"
      - "nudge: 'PR サイクル.*完了' → 空（chain 終端）"
      - "nudge: 'workflow-test-ready.*で次に進めます' → /twl:workflow-test-ready #N"
      - "nudge: #N を issue 番号で正しく置換する"
      - "nudge: マッチしないテキストには空文字を返す"

e2e: []

notes: |
  check_and_nudge() は autopilot-orchestrator.sh に埋め込まれた bash 関数のため、
  tmux 依存部分を除いたコマンド選択ロジックを test double (nudge-dispatch.sh) で検証。
  MAX_NUDGE 上限チェックは既存の orchestrator ロジックを変更しないため対象外。
