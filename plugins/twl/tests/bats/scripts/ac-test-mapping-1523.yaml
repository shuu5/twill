# ac-test-mapping-1523.yaml
# Issue #1523: tech-debt(su-observer): co-explore MENU-READY → observer 応答 ≤60s の動的 SLA bats test 追加
# (#1508 AC5 残)
# AC -> test mapping (TDD RED phase)
# Generated by ac-scaffold-tests agent
issue: 1523
framework: bats
test_file: "plugins/twl/tests/bats/issue-1508-co-explore-sla-timing.bats"
mappings:
  - ac_index: 1
    ac_text: "mock tmux セッションで wt-co-explore-* window を bats 環境で模擬できる（setup 関数でモック環境を構築）"
    test_file: "plugins/twl/tests/bats/issue-1508-co-explore-sla-timing.bats"
    test_names:
      - "ac1: bats-isolated mock tmux session exists with wt-co-explore-bats-PID window"
      - "ac1: mock window exists only in bats session not in global window list"
      - "ac1: cld-observe-any has co-explore-sla flag implemented"
      - "ac1: cld-observe-any with bats-only pattern finds mock window in bats session"
    impl_files:
      - "plugins/session/scripts/cld-observe-any"
    # impl_files 補足: setup 関数に tmux new-session / new-window の実装が必要。
    #   cld-observe-any に --co-explore-sla フラグ追加も必要（test 3）。

  - ac_index: 2
    ac_text: ".supervisor/events/ ディレクトリに MENU-READY event ファイルを書き込む処理が bats で実行できる"
    test_file: "plugins/twl/tests/bats/issue-1508-co-explore-sla-timing.bats"
    test_names:
      - "ac2: co-explore-menu-event-writer.sh script exists"
      - "ac2: co-explore-menu-event-writer.sh writes MENU-READY json to events dir"
      - "ac2: MENU-READY event JSON has sla_deadline_epoch field for 60s SLA tracking"
      - "ac2: MENU-READY event JSON from cld-observe-any has co_explore_sla_t0 field"
    impl_files:
      - "plugins/twl/skills/su-observer/scripts/co-explore-menu-event-writer.sh"
      - "plugins/session/scripts/cld-observe-any"
    # impl_files 補足:
    #   co-explore-menu-event-writer.sh は未実装（新規作成が必要）。
    #   cld-observe-any の emit_event() に co_explore_sla_t0 フィールド追加が必要。

  - ac_index: 3
    ac_text: "cld-observe-any または dedicated Monitor が MENU-READY イベントを 60s 以内に検知することを bats で time 計測して検証する"
    test_file: "plugins/twl/tests/bats/issue-1508-co-explore-sla-timing.bats"
    test_names:
      - "ac3: co-explore-menu-watcher.sh dedicated monitor script exists and is executable"
      - "ac3: dedicated monitor detects MENU-READY in bats mock session within 60s"
      - "ac3: cld-observe-any emits MENU-READY for bats-only window pattern within 60s"
      - "ac3: channels-md SLA entry in co-explore Monitor term column and dedicated monitor exists"
    impl_files:
      - "plugins/twl/skills/su-observer/scripts/co-explore-menu-watcher.sh"
      - "plugins/session/scripts/cld-observe-any"
      - "plugins/twl/skills/su-observer/refs/su-observer-supervise-channels.md"
    # impl_files 補足:
    #   co-explore-menu-watcher.sh は未実装（新規作成が必要）。
    #   --session / --window-pattern / --event-dir オプションが必要。
    #   bats mock session 作成（setup 関数実装）とセットで GREEN になる。
