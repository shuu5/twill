mappings:
  - ac_index: 1
    ac_text: "mcp__twl__twl_audit 結果で co-autopilot の controller_size ≤ 200 lines"
    test_file: "tests/bats/co-autopilot-skillmd-split.bats"
    test_name: "ac1: co-autopilot SKILL.md body lines ≤ 200 (frontmatter excluded)"

  - ac_index: 2
    ac_text: "mcp__twl__twl_audit 結果で co-autopilot の token_bloat ≤ 1500 tok"
    test_file: "tests/bats/co-autopilot-skillmd-split.bats"
    test_name: "ac2: co-autopilot SKILL.md token count ≤ 1500"

  - ac_index: 3
    ac_text: "find refs/ -name '*.md' で列挙した全 ref が SKILL.md の Read 指示文から 1:1 参照"
    test_file: "tests/bats/co-autopilot-skillmd-split.bats"
    test_name: "ac3: refs/ directory exists"

  - ac_index: 3
    ac_text: "全 ref が Read 指示文から 1:1 参照（差集合が空）"
    test_file: "tests/bats/co-autopilot-skillmd-split.bats"
    test_name: "ac3: all refs/ files are referenced by Read instructions in SKILL.md (no orphan refs)"

  - ac_index: 3
    ac_text: "SKILL.md の Read 指示文が存在しない refs/ ファイルを指さない"
    test_file: "tests/bats/co-autopilot-skillmd-split.bats"
    test_name: "ac3: no Read instructions in SKILL.md point to nonexistent refs/ files"

  - ac_index: 4
    ac_text: "refs/ 配下の各 ref が 1 トピック原則を守る（≤ 200 lines）"
    test_file: "tests/bats/co-autopilot-skillmd-split.bats"
    test_name: "ac4: each ref file in refs/ has ≤ 200 lines"

  - ac_index: 5
    ac_text: "deps.yaml に新規 ref ファイル反映（twl check --deps-integrity で 0 errors）"
    test_file: "tests/bats/co-autopilot-skillmd-split.bats"
    test_name: "ac5: twl check --deps-integrity passes with 0 errors"

  - ac_index: 6
    ac_text: "twl update-readme 実行後 README.md に新構造（refs/ 配下のファイル一覧）が反映される"
    test_file: "tests/bats/co-autopilot-skillmd-split.bats"
    test_name: "ac6: README.md contains co-autopilot refs/ section after update-readme"

  - ac_index: 7
    ac_text: "動作変更なし smoke: autopilot-launch-status-gate.bats PASS"
    test_file: "tests/bats/co-autopilot-skillmd-split.bats"
    test_name: "ac7: autopilot-launch-status-gate.bats passes (no behavior regression)"
