## 受け入れ基準（Issue #85）

1. 単一リポジトリ時に Worker の tmux 環境に `AUTOPILOT_DIR` が設定される（`tmux show-environment -t ap-#N | grep AUTOPILOT_DIR` で検証）
2. Worker の `state-read.sh` が Pilot と同じ `.autopilot/` ディレクトリを参照する
3. Worker が `IS_AUTOPILOT=true` と正しく判定する
4. openspec scenario が PASS する
