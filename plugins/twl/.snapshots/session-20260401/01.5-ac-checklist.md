## 受け入れ基準（Issue #86）

1. `IS_AUTOPILOT=false` 誤判定時でも、Pilot 側 `.autopilot/` に `issue-{N}.json` が存在すれば Worker の merge を防止する
2. 非 autopilot 通常利用（`issue-{N}.json` なし + `ISSUE_NUM` 未設定）で既存 merge フローが正常動作する
3. openspec scenario で以下が検証される:
4. `loom check` が PASS
