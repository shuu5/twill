## 受け入れ基準（Issue #97）

1. `shuu5/loom-plugin-session` リポジトリが bare repo + main worktree 構成で存在する
2. deps.yaml v3 に全コンポーネント（scripts 7件 + skills 3件）が登録されている
3. `loom check` が Missing 0 で PASS する
4. `loom validate` が Violations 0 で PASS する
5. session-state.sh の query/wait/list 各サブコマンドがテストで検証されている
6. cld-spawn, cld-observe が tmux 環境で動作する（スモークテスト）
