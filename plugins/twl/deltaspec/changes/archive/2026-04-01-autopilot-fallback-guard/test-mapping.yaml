change_id: autopilot-fallback-guard
generated_at: "2026-04-01T00:43:43+09:00"
test_framework: bash-scenario
scenarios:

  # ---------------------------------------------------------------------------
  # Requirement: auto-merge フォールバックガード（issue-{N}.json 直接確認）
  # ---------------------------------------------------------------------------

  - id: "is-autopilot-false-json-exists"
    name: "IS_AUTOPILOT=false 誤判定 + issue-{N}.json 存在"
    spec_file: "specs/fallback-guard.md"
    spec_line: 9
    requirement: "auto-merge フォールバックガード（issue-{N}.json 直接確認）"
    test_file: "tests/scenarios/autopilot-fallback-guard.test.sh"
    test_name: "IS_AUTOPILOT=false + issue-N.json 存在 → フォールバックガード発動 (block)"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: null

  - id: "is-autopilot-false-json-absent"
    name: "IS_AUTOPILOT=false + issue-{N}.json 不在（通常利用）"
    spec_file: "specs/fallback-guard.md"
    spec_line: 13
    requirement: "auto-merge フォールバックガード（issue-{N}.json 直接確認）"
    test_file: "tests/scenarios/autopilot-fallback-guard.test.sh"
    test_name: "IS_AUTOPILOT=false + issue-N.json 不在 → 通常フローに続行 (continue)"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: null

  - id: "issue-num-unset"
    name: "ISSUE_NUM 未設定（通常利用）"
    spec_file: "specs/fallback-guard.md"
    spec_line: 17
    requirement: "auto-merge フォールバックガード（issue-{N}.json 直接確認）"
    test_file: "tests/scenarios/autopilot-fallback-guard.test.sh"
    test_name: "ISSUE_NUM 未設定（空文字列）→ フォールバックチェックをスキップして続行"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: null

  - id: "autopilot-dir-propagation-bug-fallback"
    name: "AUTOPILOT_DIR 伝搬バグによる誤判定からのフォールバック防止"
    spec_file: "specs/fallback-guard.md"
    spec_line: 43
    requirement: "auto-merge-guard 誤判定シナリオ追加"
    test_file: "tests/scenarios/autopilot-fallback-guard.test.sh"
    test_name: "AUTOPILOT_DIR 伝搬バグ誤判定 + main worktree issue-N.json 存在 → ガード発動"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: null

  # ---------------------------------------------------------------------------
  # Requirement: merge-gate-execute Worker ロール検出ガード
  # ---------------------------------------------------------------------------

  - id: "worker-tmux-window-merge-rejected"
    name: "autopilot Worker tmux window からの merge 実行"
    spec_file: "specs/fallback-guard.md"
    spec_line: 25
    requirement: "merge-gate-execute Worker ロール検出ガード"
    test_file: "tests/scenarios/merge-gate-execute-worker-guard.test.sh"
    test_name: "tmux window 名 'ap-#86' → Worker ガードが merge を拒否する (exit 1)"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: null

  - id: "non-worker-tmux-window-merge-allowed"
    name: "非 autopilot tmux window からの merge 実行"
    spec_file: "specs/fallback-guard.md"
    spec_line: 29
    requirement: "merge-gate-execute Worker ロール検出ガード"
    test_file: "tests/scenarios/merge-gate-execute-worker-guard.test.sh"
    test_name: "tmux window 名 'main' → Worker ガードが続行を許可する"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: null

  - id: "outside-tmux-merge-allowed"
    name: "tmux 外からの merge 実行"
    spec_file: "specs/fallback-guard.md"
    spec_line: 33
    requirement: "merge-gate-execute Worker ロール検出ガード"
    test_file: "tests/scenarios/merge-gate-execute-worker-guard.test.sh"
    test_name: "tmux セッション外（display-message 失敗）→ Worker ガードをスキップして続行"
    type: unit
    criteria: []
    status: pending
    last_run: null
    failure_reason: null
