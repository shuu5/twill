digraph Dependencies {
    // Graph settings
    rankdir=LR;
    ranksep=0.8;
    nodesep=0.3;
    fontname="Helvetica";
    node [fontname="Helvetica", fontsize=10];
    edge [fontname="Helvetica", fontsize=9];
    label="workflow-plugin-create";
    labelloc=t;
    fontsize=14;

    // L0: Controller Skills

    // L0: Launchers

    // L0.5: Workflow Skills
    skill_workflow_plugin_create [label="twl:workflow-plugin-create\n(558 tok)", shape=ellipse, style=filled, fillcolor="#e8f5e9"];

    // Reference Skills

    // L1: Direct Commands
    cmd_plugin_interview [label="plugin-interview\n(1,086 tok)", shape=box, style=filled, fillcolor="#e3f2fd"];
    cmd_plugin_research [label="plugin-research\n(655 tok)", shape=box, style=filled, fillcolor="#e3f2fd"];
    cmd_plugin_design [label="plugin-design\n(1,192 tok)", shape=box, style=filled, fillcolor="#e3f2fd"];
    cmd_plugin_generate [label="plugin-generate\n(2,082 tok)", shape=box, style=filled, fillcolor="#e3f2fd"];

    // L2: Sub Commands

    // L3: Agents

    // L3.5: Scripts

    // L4: External

    // Layer constraints
    { rank=same; skill_workflow_plugin_create; }
    { rank=same; cmd_plugin_interview; cmd_plugin_research; cmd_plugin_design; cmd_plugin_generate; }

    // Edges
    skill_workflow_plugin_create -> cmd_plugin_interview;
    skill_workflow_plugin_create -> cmd_plugin_research;
    skill_workflow_plugin_create -> cmd_plugin_design;
    skill_workflow_plugin_create -> cmd_plugin_generate;

    // Ordering constraints (invisible edges)
    cmd_plugin_interview -> cmd_plugin_research [style=invis, weight=10];
    cmd_plugin_research -> cmd_plugin_design [style=invis, weight=10];
    cmd_plugin_design -> cmd_plugin_generate [style=invis, weight=10];

}