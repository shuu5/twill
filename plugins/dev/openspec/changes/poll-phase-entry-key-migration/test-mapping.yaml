change: poll-phase-entry-key-migration
generated_at: "2026-04-04"
mappings:
  - spec: specs/poll-phase-entry-key.md
    tests:
      - type: unit
        path: tests/bats/scripts/poll-phase-entry-key-migration.bats
        scenarios:
          - "単一リポで poll_phase を実行する"
          - "クロスリポで同一番号の Issue が同一 Phase に存在する"
          - "Issue が done/failed になった後の二重クリーンアップ防止"
          - "クロスリポ Issue の状態読み込み"
          - "単一リポ Issue の状態読み込み"
          - "単一リポでのウィンドウ名生成"
          - "クロスリポでのウィンドウ名生成"
