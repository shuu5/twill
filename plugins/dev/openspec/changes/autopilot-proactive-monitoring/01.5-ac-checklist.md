## 受け入れ基準（Issue #79）

1. Pilot が Worker の異常（3パターン）を検知し、`.autopilot/health-reports/` にレポートを出力する
2. 異常検知の閾値が設定可能（デフォルト: input-waiting 5分、chain 停止 10分）
3. レポートに Issue Draft テンプレート（タイトル、概要、再現状況）が含まれる
4. autopilot-poll.md の既存 crash-detect との重複がない（crash-detect = プロセス死亡、本 Issue = 論理的異常）
