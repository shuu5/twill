## 受け入れ基準（Issue #98）

1. 4 コンポーネントの calls から `external`/`path`/`optional`/`note` キーを除去
2. session plugin への参照形式に置換（loom #D の cross-plugin 参照構文使用）
3. script→script violations 解消（loom types.yaml 修正後）
4. `loom validate` が Violations 0 で PASS
5. `loom check` が Missing 0 を維持
