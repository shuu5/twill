## 受け入れ基準（Issue #81）

1. P1: autopilot Worker に CLAUDE_AUTOCOMPACT_PCT_OVERRIDE が環境変数として設定される
2. P2: AskUserQuestion 自動応答が `hooks/hooks.json` に設定され、autopilot で動作する
3. P3: 利用可能な新 hook が `hooks/hooks.json` に設定される（利用不可の場合はスキップし Issue にコメント）
