## 受け入れ基準（Issue #123）

1. `scripts/autopilot-orchestrator.sh` が存在する
2. Phase ループ・ポーリング・merge-gate・window 管理が script 内で完結する
3. chain 遷移停止を検知して自動 nudge する機能がある
4. crash-detect と window kill の順序競合が解消されている
5. Pilot LLM の責務が「計画承認」「retrospective」「cross-issue」のみに限定されている
6. co-autopilot SKILL.md が orchestrator 呼び出しに更新されている
7. 非 autopilot 時の手動実行パスが影響を受けない
