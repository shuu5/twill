## Phase 3: 運用・改善（進行中）

スイッチオーバー完了後の運用改善・tech-debt 解消。

## Scope

Project Board: loom-dev-ecosystem (#3)

## Issues

Project Board の Status=Todo で動的管理。固定リストではなく継続的に追加される。

```bash
gh project item-list 3 --owner shuu5 --format json --limit 200 \
  | jq -r '.items[] | select(.status != "Done") | "\(.content.number) [\(.status)] \(.content.title)"'
```

### 重点領域

| 領域 | 内容 | 対象 Context |
|------|------|-------------|
| クロスリポ対応 | orchestrator/cleanup の gh API フォールバック | Autopilot, Project Management |
| セキュリティ | プロンプトインジェクション対策の機械的強制化 | Issue Management |
| 品質改善 | specialist の誤判定修正 | PR Cycle |
| 監視強化 | health-check/crash-detect の統合 | Autopilot |

## Implementation Status

運用フェーズのため固定テーブルなし。Project Board が SSOT。

## 運用ルール

- Phase 3 以降は固定スコープの Phase ではなく、継続的な改善
- tech-debt Issue は `workflow-tech-debt-triage` で定期棚卸し
- self-improve Issue は autopilot セッション後に自動起票
- architecture spec は変更に応じて co-architect で更新
