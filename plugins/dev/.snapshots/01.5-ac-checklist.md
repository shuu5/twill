## 受け入れ基準（Issue #122）

1. `scripts/auto-merge.sh` が存在し、全 4 Layer のガードが実装されている
2. autopilot Worker が auto-merge を実行した際、IS_AUTOPILOT=true で merge-ready に遷移する（merge 禁止）
3. Layer 2/3 ガードが auto-merge.sh で機能する（worktrees/ CWD / ap-#N window で拒否）
4. AUTOPILOT_DIR が未設定でも Layer 4 フォールバックが確実に動作する
5. 非 autopilot 実行時の従来動作（squash merge + worktree 削除）が維持される
6. auto-merge.md が script 呼び出しに簡素化されている
7. テスト: 全 Layer のガードテスト + 正常 merge テスト
8. #119 に「auto-merge を対象に追加」のコメントが投稿されている
