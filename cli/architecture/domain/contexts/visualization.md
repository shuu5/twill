## Name
Visualization

## Responsibility
依存グラフの可視化（Graphviz/Mermaid/Tree/Rich）、README への SVG 埋め込み

## Key Entities
- Graph, Node, Edge, SVG, Subgraph

## Dependencies
- Plugin Structure (upstream): コンポーネントグラフを入力として受け取る
