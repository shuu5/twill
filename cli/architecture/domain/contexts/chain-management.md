## Name
Chain Management

## Responsibility
v3.0 chains の定義、chain generate（テンプレート生成）、chain validate（整合性検証）

## Key Entities
- Chain, Step, StepIn, TemplateA, TemplateB, TemplateC

## Dependencies
- Plugin Structure (upstream): コンポーネント定義を参照
- Type System (upstream): chain-type-guard で型制約を検証
