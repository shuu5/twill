## Phase 3: deltaspec 統合 + 削除 [COMPLETED]

deltaspec（bash 900行）の全機能を Python `src/twl/spec/` モジュールとして実装し、`twl spec <cmd>` サブコマンドを追加する。統合完了後、`cli/deltaspec/` を削除する。

> 完了: PR #38 (spec モジュール実装), #37 (deltaspec 削除 + symlink クリーンアップ)

## 前提

Phase 2 完了（パッケージ構造 + CLI 一本化）、ADR-0004（deltaspec 統合）

## Scope

### 3A: spec/ モジュール実装

| タスク | 説明 | 依存 |
|-------|------|------|
| src/twl/spec/__init__.py | Spec Management モジュール初期化 | Phase 2 |
| src/twl/spec/paths.py | openspec root 探索、パス解決ユーティリティ | - |
| src/twl/spec/new.py | `twl spec new <name>`: Change ディレクトリ作成 | paths.py |
| src/twl/spec/status.py | `twl spec status <name>`: Artifact 完了状態表示（JSON対応） | paths.py |
| src/twl/spec/list.py | `twl spec list`: 全 Change 一覧（ソート・JSON対応） | paths.py |
| src/twl/spec/archive.py | `twl spec archive <name>`: アーカイブ + spec統合 | paths.py |
| src/twl/spec/validate.py | `twl spec validate [name]`: delta spec 構文検証 | paths.py |
| src/twl/spec/instructions.py | `twl spec instructions <artifact> <name>`: 指示・テンプレート出力 | paths.py |

### 3B: CLI 統合

| タスク | 説明 | 依存 |
|-------|------|------|
| cli.py に spec サブコマンド追加 | `twl spec` グループと6サブコマンドを argparse に追加 | 3A |

### 3C: テスト

| タスク | 説明 | 依存 |
|-------|------|------|
| spec/ モジュールの pytest テスト | 各サブコマンドの単体テスト + 結合テスト | 3A, 3B |

### 3D: deltaspec 削除

| タスク | 説明 | 依存 |
|-------|------|------|
| cli/deltaspec/ 削除 | ディレクトリごと削除 | 3C |
| symlink 更新 | ~/.local/bin/deltaspec のリンクがあれば削除 | 3D |
| CLAUDE.md 更新 | deltaspec への参照を twl spec に変更 | 3D |

## 依存関係

```
3A → 3B → 3C → 3D
```

## 完了基準

- `twl spec new/status/list/archive/validate/instructions` が全て動作
- deltaspec と同等の JSON 出力に対応
- `cli/deltaspec/` が削除済み
- pytest で spec 関連テスト全通過
