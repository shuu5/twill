## Phase 2: パッケージ構造移行 + CLI 一本化 [COMPLETED]

twl-engine.py（6,074行）を src/twl/ パッケージに分割し、CLI エントリポイントを Python argparse に一本化する。

> 完了: PR #35 (pyproject.toml), #43 (モジュール分割), #33 (エントリポイント一本化 + twl-engine.py 削除)

## 前提

ADR-0003（パッケージ構造）, ADR-0005（Python エントリポイント）

## Scope

### 2A: パッケージ基盤構築

| タスク | 説明 | 依存 |
|-------|------|------|
| pyproject.toml 作成 | パッケージメタデータ、依存定義、scripts エントリポイント | - |
| src/twl/__init__.py | パッケージ初期化 | - |
| src/twl/__main__.py | `python3 -m twl` エントリポイント | - |
| src/twl/cli.py | argparse サブコマンド定義。全コマンドのルーティング | - |

### 2B: core/ モジュール抽出

| タスク | 説明 | 依存 |
|-------|------|------|
| src/twl/core/types.py | Type System: types.yaml ロード、TYPE_RULES、load_type_rules() | 2A |
| src/twl/core/plugin.py | Plugin Structure: deps.yaml ロード、build_graph()、find_node() | 2A, types.py |
| src/twl/core/graph.py | グラフ走査ユーティリティ: get_reverse_dependencies()、DFS/BFS | 2A |

### 2C: 機能モジュール抽出

| タスク | 説明 | 依存 |
|-------|------|------|
| src/twl/validation/ | check, validate, deep-validate, audit の4モジュール | 2B |
| src/twl/chain/ | chain generate, chain validate | 2B |
| src/twl/viz/ | graphviz, mermaid, tree 出力 | 2B |
| src/twl/refactor/ | rename, promote | 2B |

### 2D: CLI 一本化 + bash wrapper 簡素化

| タスク | 説明 | 依存 |
|-------|------|------|
| cli.py にサブコマンド接続 | 全モジュールを argparse サブコマンドから呼び出し | 2C |
| twl bash wrapper 簡素化 | `exec python3 -m twl "$@"` に書き換え | 2C |
| twl-engine.py 削除 | 全機能の移行確認後に削除 | 2C, 2D |

### 2E: テスト移行

| タスク | 説明 | 依存 |
|-------|------|------|
| 既存テストの import パス更新 | twl-engine.py → src/twl モジュール参照に変更 | 2C |
| 全テスト通過確認 | pytest tests/ で既存テスト全通過 | 2E |

## 依存関係

```
2A → 2B → 2C → 2D → 2E
```

厳密な直列。各ステップの完了確認（テスト通過）後に次へ進む。

## 完了基準

- `python3 -m twl <cmd>` で全既存コマンドが動作
- `pytest tests/` で全既存テストが通過
- twl-engine.py が削除済み
- bash wrapper が極薄（3行以内）
